$OpenBSD$
--- mozilla/nsprpub/lib/tests/Makefile.in.orig	Fri Jun 15 10:13:23 2012
+++ mozilla/nsprpub/lib/tests/Makefile.in	Fri Jun 15 10:13:39 2012
@@ -111,6 +111,12 @@ ifeq ($(OS_ARCH), Linux)
     endif
 endif
 
+ifeq (,$(filter-out Bitrig,$(OS_ARCH)))
+    ifeq ($(USE_PTHREADS),1)
+        EXTRA_LIBS = -lpthread
+    endif
+endif
+
 ifeq (,$(filter-out OpenBSD,$(OS_ARCH)))
     ifeq ($(USE_PTHREADS),1)
         EXTRA_LIBS = -lpthread
