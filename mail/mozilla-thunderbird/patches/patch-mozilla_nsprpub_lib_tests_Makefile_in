$OpenBSD$
--- mozilla/nsprpub/lib/tests/Makefile.in.orig	Fri Sep  7 16:06:21 2012
+++ mozilla/nsprpub/lib/tests/Makefile.in	Wed Oct 10 05:47:18 2012
@@ -79,6 +79,12 @@ ifeq ($(OS_ARCH), Linux)
     endif
 endif
 
+ifeq (,$(filter-out Bitrig,$(OS_ARCH)))
+    ifeq ($(USE_PTHREADS),1)
+        EXTRA_LIBS = -lpthread
+    endif
+endif
+
 ifeq (,$(filter-out OpenBSD,$(OS_ARCH)))
     ifeq ($(USE_PTHREADS),1)
         EXTRA_LIBS = -lpthread
