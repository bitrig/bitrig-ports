$OpenBSD$
--- mozilla/nsprpub/lib/tests/Makefile.in.orig	Sun Nov 25 14:34:21 2012
+++ mozilla/nsprpub/lib/tests/Makefile.in	Sun Nov 25 14:47:29 2012
@@ -85,6 +85,12 @@ ifeq (,$(filter-out OpenBSD,$(OS_ARCH)))
     endif
 endif
 
+ifeq (,$(filter-out Bitrig,$(OS_ARCH)))
+    ifeq ($(USE_PTHREADS),1)
+        EXTRA_LIBS = -lpthread
+    endif
+endif
+
 ifeq ($(OS_ARCH), OSF1)
 LDOPTS += -rpath $(PWD)/$(dist_libdir) -lpthread
 endif
