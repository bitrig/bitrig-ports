--- Makefile.orig	Thu Jun 19 02:48:42 2014
+++ Makefile	Thu Jun 19 02:51:15 2014
@@ -223,6 +223,17 @@ endif
 endif
 endif
 
+ifndef D2U_OS
+ifeq (Bitrig, $(shell uname -s))
+	D2U_OS =bitrig
+ifdef ENABLE_NLS
+	CFLAGS_OS     = -I/usr/local/include
+	LDFLAGS_EXTRA = -L/usr/local/lib
+	LIBS_EXTRA    = -lintl
+endif
+endif
+endif
+
 ifeq (Darwin, $(shell uname -s))
 	D2U_OS = Darwin
 ifdef ENABLE_NLS
