$OpenBSD$
--- Source/WTF/wtf/NumberOfCores.cpp.orig	Thu Jul 19 06:02:14 2012
+++ Source/WTF/wtf/NumberOfCores.cpp	Thu Apr 18 13:58:08 2013
@@ -26,7 +26,7 @@
 #include "config.h"
 #include "NumberOfCores.h"
 
-#if OS(DARWIN) || OS(OPENBSD) || OS(NETBSD) || OS(FREEBSD)
+#if OS(DARWIN) || OS(OPENBSD) || OS(BITRIG) || OS(NETBSD) || OS(FREEBSD)
 #include <sys/param.h>
 // sys/types.h must come before sys/sysctl.h because the latter uses
 // data types defined in the former. See sysctl(3) and style(9).
@@ -53,7 +53,7 @@ int numberOfProcessorCores()
     if (s_numberOfCores > 0)
         return s_numberOfCores;
 
-#if OS(DARWIN) || OS(OPENBSD) || OS(NETBSD) || OS(FREEBSD)
+#if OS(DARWIN) || OS(OPENBSD) || OS(BITRIG) || OS(NETBSD) || OS(FREEBSD)
     unsigned result;
     size_t length = sizeof(result);
     int name[] = {
