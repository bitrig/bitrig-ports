$OpenBSD$
--- nsprpub/lib/tests/Makefile.in.orig	Sun Nov 25 11:31:22 2012
+++ nsprpub/lib/tests/Makefile.in	Sun Nov 25 11:45:02 2012
@@ -85,6 +85,12 @@ ifeq (,$(filter-out OpenBSD,$(OS_ARCH)))
     endif
 endif
 
+ifeq (,$(filter-out Bitrig,$(OS_ARCH)))
+    ifeq ($(USE_PTHREADS),1)
+        EXTRA_LIBS = -lpthread
+    endif
+endif
+
 ifeq ($(OS_ARCH), OSF1)
 LDOPTS += -rpath $(PWD)/$(dist_libdir) -lpthread
 endif
