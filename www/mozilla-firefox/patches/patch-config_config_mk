$OpenBSD$
--- config/config.mk.orig	Sun Nov 25 11:31:23 2012
+++ config/config.mk	Sun Nov 25 11:48:43 2012
@@ -92,7 +92,7 @@ AUTOCONF_TOOLS	= $(topsrcdir)/build/autoconf
 # but save the version to allow multiple versions of the same base
 # platform to be built in the same tree.
 #
-ifneq (,$(filter FreeBSD HP-UX Linux NetBSD OpenBSD SunOS,$(OS_ARCH)))
+ifneq (,$(filter FreeBSD HP-UX Linux NetBSD OpenBSD Bitrig SunOS,$(OS_ARCH)))
 OS_RELEASE	:= $(basename $(OS_RELEASE))
 
 # Allow the user to ignore the OS_VERSION, which is usually irrelevant.
