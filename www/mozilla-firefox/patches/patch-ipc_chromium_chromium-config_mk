$OpenBSD$
--- ipc/chromium/chromium-config.mk.orig	Wed Sep 11 01:15:04 2013
+++ ipc/chromium/chromium-config.mk	Fri Sep 27 23:35:37 2013
@@ -103,12 +103,24 @@ DEFINES += \
   $(NULL)
 
 else # } {
+ifeq ($(OS_ARCH),Bitrig) # {
 
+OS_OPENBSD = 1
+OS_BSD = 1
+OS_LIBS += $(call EXPAND_LIBNAME,kvm)
+DEFINES += \
+  -DOS_OPENBSD=1 \
+  -DOS_BSD=1 \
+  $(NULL)
+
+else # } {
+
 OS_LINUX = 1
 DEFINES += \
   -DOS_LINUX=1 \
   $(NULL)
 
+endif # }
 endif # }
 endif # }
 endif # }
