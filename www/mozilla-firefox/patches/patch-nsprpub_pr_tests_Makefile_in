$OpenBSD$
--- nsprpub/pr/tests/Makefile.in.orig	Sun Nov 25 11:31:22 2012
+++ nsprpub/pr/tests/Makefile.in	Sun Nov 25 11:45:56 2012
@@ -395,6 +395,12 @@ LDOPTS += -pthread
 endif
 endif
 
+ifeq ($(OS_ARCH),Bitrig)
+ifeq ($(USE_PTHREADS),1)
+LDOPTS += -pthread
+endif
+endif
+
 ifeq ($(OS_ARCH),BSD_OS)
 ifneq ($(OS_RELEASE),1.1)
 EXTRA_LIBS = -ldl
@@ -410,7 +416,7 @@ LIBPTHREAD = -lpthread
 ifeq ($(OS_ARCH),AIX)
 LIBPTHREAD = -lpthreads
 endif
-ifeq (,$(filter-out FreeBSD OpenBSD BSD_OS QNX Darwin OpenUNIX,$(OS_ARCH)))
+ifeq (,$(filter-out FreeBSD OpenBSD Bitrig BSD_OS QNX Darwin OpenUNIX,$(OS_ARCH)))
 LIBPTHREAD =
 endif
 ifeq ($(OS_ARCH)$(basename $(OS_RELEASE)),HP-UXB.10)
