$OpenBSD$
--- security/nss/lib/freebl/Makefile.orig	Fri Oct 11 20:41:37 2013
+++ security/nss/lib/freebl/Makefile	Fri Oct 11 20:47:09 2013
@@ -229,7 +229,7 @@ endif
 # to bind the blapi function references in FREEBLVector vector
 # (ldvector.c) to the blapi functions defined in the freebl
 # shared libraries.
-ifeq (,$(filter-out BSD_OS FreeBSD Linux NetBSD OpenBSD, $(OS_TARGET)))
+ifeq (,$(filter-out BSD_OS FreeBSD Linux NetBSD OpenBSD Bitrig, $(OS_TARGET)))
     MKSHLIB += -Wl,-Bsymbolic
 endif
 
