$OpenBSD$

stc++ has isfinite and signbite. Both of them are defined inline, not as macro.

--- third_party/WebKit/Source/wtf/MathExtras.h.orig.port	Wed May 14 15:27:29 2014
+++ third_party/WebKit/Source/wtf/MathExtras.h	Sat May 31 08:55:46 2014
@@ -61,7 +61,7 @@ inline double wtf_ceil(double x) { return copysign(cei
 
 #endif
 
-#if OS(OPENBSD)
+#if OS(OPENBSD) && !defined(__Bitrig__)
 
 namespace std {
 
